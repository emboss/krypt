source :rubygems

gem 'krypt-core', :path => File.expand_path('../krypt-core', File.dirname(__FILE__))

group :development do
  gem 'rake'
  gem 'rspec'
end

group :test do
  gem 'simplecov', :require => false
end

gemspec
